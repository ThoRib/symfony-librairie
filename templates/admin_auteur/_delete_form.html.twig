<form method="post" action="{{ path('app_admin_auteur_delete', {'id': auteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auteur.id) }}">
    <button class="btn btn-danger mt-2"><i class="align-middle" data-feather="trash-2"></i></button>
</form>
