{% extends 'base-backend.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1>Accueil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ home.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ home.texte|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_home_index') }}" class="btn btn-dark">Retour à la liste</a>

    <a href="{{ path('app_admin_home_edit', {'id': home.id}) }}" class="btn btn-success ms-2"><i class="align-middle" data-feather="pen-tool"></i></a>

    {{ include('admin_home/_delete_form.html.twig') }}
{% endblock %}
